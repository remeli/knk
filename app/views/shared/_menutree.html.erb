 <ul id="menutree">
    <% for category in @main_categories %>
      <li class="closed">
        <span class="folder">
          <%= link_to category.name, category %>
        </span>
        <%= raw find_all_subcategories(category) %>
      </li>
    <% end %>
  </ul>